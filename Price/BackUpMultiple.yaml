---
- hosts: leafs
  connection: local
  gather_facts: no

  tasks:
  - name: "Create Vlan"
    dellos9_config:
      lines:
         - interface vlan 999
         - test
         
 #   register: show_ver
 
 # - debug: var=show_ver
