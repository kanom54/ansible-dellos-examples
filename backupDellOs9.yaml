---
 - hosts: Leaf6A 
   connection: network_cli
   gather_facts: no

   tasks:
    - name: backup dellOs9
      dellos9_config:
          backup: true
          commands: ['do show run']
          timeout: 120
          backup_options:
               dir_path: /home/ansible/ColoBackup
